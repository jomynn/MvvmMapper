<UserControl x:Class="SharedVmApp.Views.ForgotPasswordView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:SharedVmApp.ViewModels">
    <UserControl.DataContext>
        <vm:AuthViewModel />
    </UserControl.DataContext>
    <Grid>
        <TextBox Text="{Binding Email}" />
        <Button Command="{Binding ForgotPasswordCommand}" Content="Reset Password" />
    </Grid>
</UserControl>
